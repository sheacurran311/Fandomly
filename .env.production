# Production environment variables
# LANDING_ONLY is injected at build time via vite.config.ts `define`.
# Set LANDING_ONLY=true as a Replit production secret to enable landing-only mode.
# (No VITE_ prefix — Replit won't strip it.)
LANDING_ONLY=true
