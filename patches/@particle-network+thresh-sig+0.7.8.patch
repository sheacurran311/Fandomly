diff --git a/node_modules/@particle-network/thresh-sig/esm/index.js b/node_modules/@particle-network/thresh-sig/esm/index.js
index 1ae0cb1..4f84775 100644
--- a/node_modules/@particle-network/thresh-sig/esm/index.js
+++ b/node_modules/@particle-network/thresh-sig/esm/index.js
@@ -1256,7 +1256,7 @@ async function __wbg_init(input) {
   if (wasm !== void 0)
     return wasm;
   if (typeof input === "undefined") {
-    input = new URL("../wasm/thresh_sig_wasm_bg.wasm", import.meta.url);
+    input = "/particle-wasm/thresh_sig_wasm_bg.wasm";
   }
   const imports = __wbg_get_imports();
   if (typeof input === "string" || typeof Request === "function" && input instanceof Request || typeof URL === "function" && input instanceof URL) {
@@ -1380,6 +1380,8 @@ async function initWasm() {
       await thresh_sig_wasm_default();
       context.initialized = true;
     } catch (e) {
+      console.error("[Particle] Failed to initialize thresh-sig wasm", e);
+      throw e;
     }
     context.initializing = false;
   }
